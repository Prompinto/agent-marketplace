# Scenario: claim-ledger-live-acceptance

**Group:** Secondary tier (live-Codex acceptance)
**Targets:** a REAL Codex judgment, single-reviewer, real `DISPOSITION` marker round-trip -- not a
scripted fake-codex transcript. Uses the REAL `codex` CLI (no fake-codex PATH injection) and costs
real API time.

One tiny fixture, one file (`divide.py`), one independently-verifiable, objective defect stated as
a documented local requirement in a comment immediately above the code that violates it:
`safe_divide()` is required (by its own adjacent comment) to raise `ZeroDivisionError` when the
divisor is zero, but instead returns `0`.

## How to run

```bash
bash codex-stream-review/evals/scenarios/claim-ledger-live-acceptance/setup.sh
```

Then invoke `codex-stream-review:ccs` against the printed `REPO_DIR` with the REAL `codex` CLI on
`$PATH` (no fake-codex injection):

> review the uncommitted change in this fixture repo

This is a genuine, unscripted Codex judgment call. Expected real flow:

1. Round 1: Codex should find the real defect. Claude verifies it by reading the file, then
   actually fixes it (letting a genuine `ZeroDivisionError` propagate instead of the `if b == 0:
   return 0` special case).
2. Per `references/claim-ledger.md`'s "When to ask" condition (b), round 2's own focus explicitly
   names the claim_id and requests a real `DISPOSITION` confirmation.
3. Round 2: Codex re-reads the current file for real and, if it agrees, responds with a real
   `DISPOSITION <claim_id>: RESOLVED -- <reason>` marker, genuinely generated by the backend --
   Claude's parser validates it for real.
4. Convergence: `CLEAN`, with the claim's real disposition recorded as `"resolved"`.

A real disagreement (Codex maintaining `STILL OPEN`, or a genuine multi-round negotiation) is also
an acceptable outcome -- follow the real conversation wherever it genuinely goes.

```bash
bash codex-stream-review/evals/check-result.sh <result.json> claim-ledger-live-acceptance
```

## Expected result

A genuine semantic terminal state tracking the claim through to some terminal disposition --
`CLEAN` (claim resolved/retracted for real) is the expected common case for a defect this simple
and objective; `NOT_CONVERGED` is acceptable only if a real, evidence-based disagreement genuinely
survives after a genuine multi-round attempt to resolve it. `COULD_NOT_VERIFY`,
`PARTIAL_COVERAGE`, or either integrity-failure state are explicit scenario FAILURES to debug, not
acceptable results. No exact round count or wording is ever asserted. `threads`: exactly 1 entry,
`cleanup:"deleted"`. `claims`: non-empty, and if `exit_state` is `CLEAN`, every claim's own
`disposition` is `resolved` or `retracted` (never left open under a CLEAN verdict).
