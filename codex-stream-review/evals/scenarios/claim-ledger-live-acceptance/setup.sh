#!/usr/bin/env bash
# Scenario: claim-ledger-live-acceptance
# Targets: a REAL Codex judgment, single-reviewer, real DISPOSITION marker round-trip -- not a
# scripted fake-codex transcript. NO fake-codex PATH injection -- the real `codex` CLI must be on
# $PATH, unmodified.
#
# One tiny fixture, ONE file, ONE independently-verifiable, objective defect stated as a
# documented local requirement in a comment immediately above the code that violates it -- the
# same "adjacent 5 lines, not organic broad discovery" design as parallel-live-acceptance, but
# single-reviewer and specifically exercising the real claim ledger round-trip (raise -> real fix
# -> real DISPOSITION marker -> real closure) end to end against a real Codex backend.
set -euo pipefail

REPO_DIR="$(mktemp -d "/tmp/ccs-eval-claim-ledger-live-acceptance.XXXXXX")"
git -C "$REPO_DIR" init -q
git -C "$REPO_DIR" config user.email "eval@example.com"
git -C "$REPO_DIR" config user.name "ccs-eval"
printf '# fixture repo for claim-ledger-live-acceptance\n' > "$REPO_DIR/README.md"
git -C "$REPO_DIR" add README.md
git -C "$REPO_DIR" commit -q -m "initial"

cat > "$REPO_DIR/divide.py" <<'PYEOF'
def safe_divide(a, b):
    # REQUIREMENT: this function must raise ZeroDivisionError (not return a sentinel value like
    # 0 or None) when b is zero, so callers can never silently treat a division-by-zero as a
    # normal 0 result.
    if b == 0:
        return 0
    return a / b
PYEOF

echo "REPO_DIR=$REPO_DIR"
cat <<'EOF'

Next step: invoke codex-stream-review:ccs against REPO_DIR with the REAL `codex` CLI on PATH (no
fake-codex injection, no FAKE_CODEX_* env vars for this scenario). Task text: "review the
uncommitted change in this fixture repo".

Single-reviewer (2-file... actually 1-file diff, well within the "small" tier -- no parallel
dispatch here). This is a REAL Codex judgment call with NO scripted verdict -- do not assume in
advance which round it converges on, or the exact wording of any finding.

Expected real flow (genuine, not scripted):
  1. Round 1: Codex should find that safe_divide() returns 0 instead of raising
     ZeroDivisionError when b==0, contradicting the comment immediately above it. Claude verifies
     this is a real defect by reading the file, then ACTUALLY FIXES it (e.g. removing the
     "if b == 0: return 0" special case entirely, letting Python's own division raise
     ZeroDivisionError naturally, or raising it explicitly).
  2. Per claim-ledger.md's "When to ask" condition (b) (a fix was just applied in direct response
     to round 1's finding), round 2's own focus explicitly names the claim_id and requests a real
     DISPOSITION confirmation from Codex.
  3. Round 2: Codex re-reads the current file for real and (if it agrees the fix is correct)
     responds with a real "DISPOSITION <claim_id>: RESOLVED -- <reason>" marker in its own
     summary text, genuinely generated by the real backend, not scripted -- Claude's parser
     (claim-ledger.md section 4's exact current grammar) validates it for real.
  4. Convergence: CLEAN, with the claim's real disposition recorded as "resolved".

A real disagreement (Codex maintaining STILL OPEN, or a genuine multi-round negotiation) is also
an acceptable real outcome -- follow the real conversation wherever it genuinely goes, never force
a specific disposition.

Expected: a genuine SEMANTIC terminal state tracking the claim through to SOME terminal
disposition -- CLEAN (claim resolved/retracted for real) is the expected common case for a defect
this simple and objective; NOT_CONVERGED is acceptable only if a real, evidence-based
disagreement genuinely survives (do not settle for NOT_CONVERGED without first genuinely trying to
resolve the disagreement across multiple real rounds, the same way a real session would).
COULD_NOT_VERIFY, PARTIAL_COVERAGE, or either integrity-failure state are explicit scenario
FAILURES to debug, not acceptable results. Clean up the real Codex thread at the end regardless of
outcome -- no fake-codex here, so the real `codex delete --force` call must succeed for real.
EOF
